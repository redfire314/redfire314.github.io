$(document).ready(() => {
    $('.fixed-action-btn').floatingActionButton()
    $('.sidenav').sidenav()
    $('.carousel.carousel-slider').carousel({
        fullWidth: true,
        indicators: true
    })
})